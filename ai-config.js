/**
 * Zumi 임베디드 AI 음성인식 파라미터 구성 파일
 * (수업 목적에 맞게 아래 설정값을 자유롭게 변경하세요.)
 */

export const AI_CONFIG = {
    // 1. 인공지능이 매칭할 표준 로봇 명령어 리스트 (원하는 대로 개수 추가/수정 가능)
    base_commands: [
        "앞으로 가라",
        "뒤로 가라",
        "오른쪽으로 돌아",
        "왼쪽으로 돌아",
        "멈춰"
    ],

    // 2. AI 판단의 엄격한 기준 점수 (0.0 ~ 1.0)
    // 수치가 높을수록 꼼꼼하게 검사하고, 낮을수록 융통성 있게 허용합니다. (기본값: 0.45)
    threshold: 0.45,

    // 3. MQTT 기본 공용 브로커 및 포트 주소
    mqtt_broker: "wss://broker.emqx.io:8084/mqtt",

    // 4. 최초 진입 시 하단 입력창에 채워질 기본 토픽
    default_topic: "student01/cmd"
};